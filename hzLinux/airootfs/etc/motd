Welcome to hzArchiso (hzLinux live image).

Run hz-install-tui for the hzArchiso machine-profile installer.
Run hz-install --dry-run --target-disk /dev/DISK to print
the native encrypted-Btrfs install plan without installing.
