Welcome to hzArchiso (hzLinux live image).

Interactive installer (profile menu, field checks, disk confirmation):
  hz-install-tui

Dry-run install plan (no wipe) — profiles: rgx1gen11 | rgSURFLat | rgam5terra
  hz-install --dry-run --profile rgSURFLat --target-disk /dev/DISK

Docs (on the USB / git checkout): docs/orgmode/getting-started.org
