# A network that drops NTP (UDP 123) would keep this unit, time-sync.target,
# pacman-init and the keyring waiting forever. pacman-init is only ordered
# after time-sync.target, so bounding the wait lets it run.
[Service]
TimeoutStartSec=90s
